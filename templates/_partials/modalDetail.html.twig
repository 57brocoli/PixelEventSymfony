<div id="modalDet-{{ item.id }}" class="modal">
	<div class="modal-content bold">
		<span class="close">&times;</span>
		<h2 class="t-center">{{item.username}}</h2>
		<hr class='my-2'>
		<p class='colorBleu'>Role :
			{% for role in item.roles %}
				{% if role is same as('ROLE_USER') %}
					<span class='c-purple'>Utilisateur</span>
				{% elseif role is same as('ROLE_ADMIN') %}
					<span class='c-purple'>Administrateur</span>
				{% endif %}
			{% endfor %}
		</p>
		<p class='colorBleu'>Username :
			<span class='c-purple'>{{item.username}}</span>
		</p>
		<p class='colorBleu'>Email :
			<span class='c-purple'>{{item.email}}</span>
		</p>
		<p class='colorBleu'>Date d'inscription :
			<span class='c-purple'>{{item.email}}</span>
		</p>
		<p class='colorBleu'>Newsletter :
			<span class='c-purple'>{{item.isSubscriber}}</span>
		</p>
		<p class='colorBleu'>Identifiant :
			<span class='c-purple'>{{item.id}}</span>
		</p>
	</div>
</div>
